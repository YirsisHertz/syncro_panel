import type { Environment } from "./environment.interface";

export const environment: Environment = {
  unsplash: {
    application_id: 0, // number
    access_key: "", // string
    secret_key: "", // string,
    default_image: "" // string
  },
  webcard_base_url: ""
};
